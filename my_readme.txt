== Start spark in  Scala shell:
spark-shell
sh ./bin/spark-shell

==========
sbt assembly

=========
== add git init
git init


==================================
== Delete generated files under project
find . -name target -type d -exec rm -rf {} \;

git add .
git commit -m 'Spark-scala version 1'

